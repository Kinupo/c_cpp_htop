set(INCLUDE_PATH ${CMAKE_SOURCE_DIR}/include)

add_library(systemLib "")

target_include_directories(
    systemLib 
    PUBLIC
        ${INCLUDE_PATH})

target_sources(
    systemLib 
    PRIVATE
        delimited_file_reader.cpp
        property_file_reader.cpp
        linux_parser.cpp
        ${INCLUDE_PATH}/system/cpu/processor.h
        ${INCLUDE_PATH}/system/operating_system/processor.h
        ${INCLUDE_PATH}/system/cpu/processor.h)